# Changelog

本项目版本号遵循 `0.x` 阶段的语义化：`0.<minor>.<patch>`；预发布版本带 `-beta.N` 后缀（面板中显示为 `0.1.1beta1`）。

## v0.4.30 — 文档补齐到当前版本

### 文档

- **CHANGELOG 补齐 v0.4.1 → v0.4.29（24 个版本节）**：此前停在 v0.4.0，而代码已经迭代了 30 个版本 —— 从仓库完全看不出「会话背景、双语界面、焦点管理、日志脱敏与一键清空、ReDoS 防线、弹层层级修复」等大量变更。现按发布版本逐节记录（每节含新增 / 修复 / 改进与依据）。
- **README 修正 6 处硬错误**：①「本插件的操作界面有且只有中文，暂未提供英文」—— 跟随 DSH 语言的双语（zh/en）早在 v0.4.22 就已交付；② fork 版本号仍写 `0.2.0`；③ 三个控件被描述成「滑块」，实际早已是下拉菜单（v74/v76）；④ FAQ 里「界面能换英文吗 → **暂时不能**」；⑤ 署名行只有上游版本、没有本 fork 版本；⑥ 隐私段缺「会话背景」与「本地日志默认不写」两条。
- **README.en.md 同步修正 7 处**（`Chinese-only` 说明整块、`(slider)`、`@0.2.0`、FAQ 的 Tier 与语言两行、隐私段）。
- **ACCEPTANCE.md / DELIVERY.md 增加 fork 差异提示**：这两份是上游 `v0.1.1-beta.1` 的验收与交付留痕，**正文一字未改**；但其中「档位/权限是可拖动的滑块」「模型是胶囊」「玻璃模糊 + 弹层弹簧入场 + 滑块换挡弹跳 + hover 位移」「三档优化」「包名 `@dsh-external/`」等描述对本 fork 已经过时 —— 照原文验收会失败。现于两份文件顶部各加一段对照表，逐条给出上游形态与当前现状。
- **`package.json` 描述更新**：补上会话背景消歧、跟随 DSH 界面语言、键盘与焦点管理、一键清空本地诊断日志。

### 校验

- 文档 ↔ 源码反向审计（21 项能力声明逐项在源码中查证）：`useContext` / `collectSessionContext` / `syncLocale` / `EN_DICT` / `localeService` / `clear-logs` / `clearLogs` / `EVIDENCE_MAX_BYTES` / `appendBounded` / `debugEvidence` / `ALWAYS_BEACON` / `sanitizeBeaconData` / `looksLikeSecret` / `portalPop` / `ReactDOM.createPortal` / `runToolLoop` / `traceRows` / `riskyRegex` / `LOOP_GREP_BUDGET_MS` / `LOOP_GREP_MAX_PATTERN` / `MAX_REQUEST_CHARS` / `popItemKeyNav` / `TIERS` / `PERMISSIONS` / `perSession` / `persistUi` / `remount-controls` / `__DPO_HEAL__` / `ctx.inject(['settings']` … **全部命中**。捕获阶段监听定位到 `lib/client.js:1008-1009`（pointerdown + keydown）与 `lib/client.js:3032-3033`（keydown + click）的 `window.addEventListener(..., true)`。
- 版本号五处一致：`package.json` / `PLUGIN_VERSION` / CHANGELOG 顶部 / README / README.en。
- 过时串扫描全部 0 命中：`有且只有中文` / `Chinese-only` / `@0.2.0` / `（滑块）` / `(slider)` / `Tier slider`。

## v0.4.29 — DSH 设置命名空间真正注册（v149）

### 修复

- **插件设置从未真正注册进 DSH**：`apply()` 里同步调用 `ctx.get('settings')`，而依赖声明 `inject` 只列了 `webServer` / `timer`，服务未就绪时恒取到 `undefined`，命名空间注册因此从未成功（状态字段始终是 `no-settings-service`，被后续无条件写入的 `mirror-unavailable` 掩盖）。现改为 DSH 官方的可选依赖姿势 `ctx.inject(['settings'], (settingsCtx) => …)`。
- **schema 库用错**：注册契约要求的是 `@deepseek-ai/schemastery`，此前构造的是 `zod`（两库链式方法不同，`schemastery` 没有 `.optional()` / `.nullable()`，等价写法是 `.required(false)`）。现已改用 `schemastery` 并逐项改写。
- **诊断信息被覆盖**：镜像函数此前无条件把状态写成 `mirror-unavailable`，掩盖了更有信息量的失败原因；并且 `settingsScope.update()` 返回的 Promise 拒绝无法被 `try/catch` 捕获，现改为显式 `.then(onOk, onErr)`。

### 新增

- **设置反向同步**：在 DSH 设置文档里改动 `tier` / `permission` / `useContext` 会写回插件状态（通过比较当前状态收敛，天然无回路）。
- **镜像去重**：仅在 `tier` / `permission` / `model` 真正变化时才写设置文档 —— 此前每次拖动浮窗（几何落盘也走同一条路）都会 bump 设置 revision 并广播事件。浮窗几何不再镜像进设置文档（属插件内部状态）。

### 说明

- 插件**不会**在 DSH 设置的「插件」页出现独立卡片：那需要在 `settings.plugin.item` 插槽注册 React 卡片，与插件自己的控件行三菜单重复，且难以做到与 DSH 卡片逐像素一致。设置项可在设置面板顶部「打开配置文件」中查看与编辑，编辑后会同步回插件。

## v0.4.28 — 浮层自愈不再中途放弃（v148）

### 修复

- **浮层自愈定时器在启动约 3 秒后永久失效**：原实现的清理条件写成 `tries > 1` 即 `clearInterval`，而注释声明的意图是「长期防护，防止弹窗无声消失」。现在关窗只重置计数、绝不关闭定时器，并给自愈次数设上限与分档兜底。
- **插槽同 id 重复注册抛异常，导致「放弃」分支被整段跳过**：自愈重挂必然触发 `list slot … already has an entry with id …` 异常，使控件自愈的 `tries >= 8` 放弃逻辑永远执行不到，退化为每 1.2 秒无限重试 + 无限未捕获异常（上游版本中该异常的堆积量可占日志的 38%）。现在重挂被 `try/catch` 包住，且改为**优先触发重渲染**（零成本），仅每 3 次才尝试重挂一次。

## v0.4.25 — 国际化反向审计（v146-v147）

### 修复

- **切换英文后局部仍是中文**：补齐 4 处缺失的字典条目（「清理中…」「点击关闭：关闭后不再读取本会话的任何历史消息」「该 provider 未上报思考 token」「已拦截」）。
- **22 处用户可见文案未纳入翻译**：包括 `aria-label`、拦截计数 `title`、查证轨迹的「查证 / 收尾」阶段与行数、若干条通知（启动失败、优化未产出内容、按原文发出等）、**6 条错误消息映射**（未注册 provider / 已中断 / 无可用路由 / 401 / 429 / 超时）、降级面板与帮助面板的若干拼接文案。
- **浮层渲染崩溃（极端档 + 查证轨迹非空时必现）**：轨迹跳转入口所在的局部作用域里有一个同名变量遮蔽了翻译函数，触发 `TypeError: t is not a function`，被错误边界捕获成降级面板。该组合此前从未被验证覆盖，两轮未发现。

### 工程

- 同步门禁从 1 维扩到 4 维（缺 key / 未包装字面量 / 遮蔽 / 失效条目），并用正负例各 9 条做了门禁自身的测试。

## v0.4.23 — 上下文 chip 的 NaN（v145）

### 修复

- **「背景 N 轮」显示成「背景 6 轮NaN」**：批量替换只覆盖到表达式前半段，残留片段与插入的运算符组成了双重一元加号（`+ + " 轮"`），中文与英文界面都受影响。`node --check` 会放行这种语法合法的错误，只有状态级真机验证能发现。
- 补上 3 处未包装的「‹ 回退」按钮文案与控件行的「档位 / 发送」前缀。

## v0.4.22 — 跟随 DSH 的界面语言（v144）

### 新增

- **完整的中英双语**：以中文原文为字典 key，在非中文环境下由 DSH 的语言服务驱动切换。覆盖控件行、三个下拉菜单、帮助面板（5 段 19 行）、浮窗全部状态与按钮、通知、错误消息。DSH 在环境语言非中文时自身即为英文，此前插件全中文是明显的割裂。
- 语言值只在运行时读取，不落盘、不进状态文件；未收录的条目回落到中文原文，翻译可增量补齐。

### 实现要点

- 档位与发送方式的文案改为 getter，控件行标签、菜单项、帮助面板、状态行、浮窗来源行、发送通知全部零改动跟随语言。

## v0.4.21 — 诊断日志迁出依赖目录（v142）

### 修复

- 诊断日志此前落在依赖安装目录内（`node_modules/.pnpm/…`），换依赖版本或卸载会留下孤儿，历史实测攒了 7 个互不相干的目录。现改为写入用户数据目录，并对既有日志做一次性迁移。

## v0.4.20 — 诊断遥测默认静默（v141）

### 修复

- **每次按键都会向宿主发一次遥测请求**（节流 800ms）：该探针是排查输入拦截问题时临时加的，却留在了产品里。现在只有显式开启调试取证时才写日志；仅未捕获错误与自愈失败始终留证。

### 改进

- 帮助面板的「本地日志」说明改写为：默认不写任何日志，平时打字、拦截、切换档位都不会落盘或发请求。

## v0.4.19 — 注入消息过滤 + 清空日志（v140）

### 修复

- **系统注入消息被当成对话背景**：`<goal_round>` 一类带目标与轮次的系统消息不被原后缀白名单识别，会被当作「用户说过的话」喂给优化模型。现泛化为「任何 XML 风格标签开头的整条消息」并补上三条系统播报模式；用 134 条真实样本复测，漏检从 14 条降到 0 条。
- **帮助面板在矮视口下关闭按钮不可达**：内容高 1113px 而低视口下弹层仅 266px，底部按钮沉到首屏外。现底部操作区改为粘性贴底。
- **点击按钮期间焦点掉到页面根节点**：忙碌态用 `disabled` 会让浏览器把焦点移走且不归还，改用 `aria-disabled` 并保留早退守卫。
- 一条与按钮基础样式完全重复的规则被删除（它此前从未生效）。

### 新增

- **帮助面板内可清空本机诊断日志**，并显示清理条数与释放空间；宿主端尚未更新时提示「重启后可用」，不静默失败。

## v0.4.18 — 停止记录原文 + 日志有界（v139）

### 修复

- **会话消息原文被永久落盘**：诊断日志此前把每条 user 消息的前 240 字写入磁盘。现只记长度、计数、状态与指纹；仅在显式开启调试取证时才补记文本。同时该记录能把系统注入消息与真实用户输入区分开，恢复了它原本的取证价值。
- **语言服务订阅风暴**：一次页面/会话事件会让语言被连续通知约 40 次，每次都发一次遥测请求、每次都在宿主端同步写盘；实测该阶段占全部日志的 53%、间隔中位数 1ms、每秒最多 80 条、而取值只有 2 种。现在取值未变即不发。
- **所有诊断日志无上限追加**：现按 2MB 轮转，保留最近两代。

### 新增

- 遥测出口统一脱敏：绝对路径与明确的密钥前缀在写出前被替换，超长字符串截断并注明原长。

## v0.4.17 — 健壮性审计（v138）

### 修复

- **只读查证的正则可能永久卡死宿主进程**：查证工具的正则匹配是同步执行的，工具循环的 90 秒预算只能中止流式请求，无法中断同步代码 —— 一个嵌套量词正则（如 `(a+)+b`）配合长行即可让进程失去响应、只能重启。实测每多 2 个字符耗时精确 ×4（指数）。现在有四道防线：pattern 长度上限、嵌套量词拒绝并给出改写建议、逐文件累计 8 秒预算、单行匹配长度上限。用检测器 10 例（4 个危险形状 + 6 个正常模式）与真实极端档自由查证复测，零误伤。
- **输入长度无上限**：客户端在开窗前拦截并提示具体字数与上限，宿主端同样返回 400，避免优化一大段超长文本。

## v0.4.16 — 浮窗焦点兜底（v137）

### 修复

- 点「重新生成」后焦点留在页面根节点（输入框已出现但没进去）；点「取消」后同样丢失。现在焦点进入重生成输入框、取消后回到「重新生成」按钮。
- 浮窗容器补 `tabindex="-1"`（可编程聚焦、不进 Tab 序列）与去除聚焦轮廓，并加一层兜底：状态变化后若焦点落在页面根节点，自动移到浮窗容器。对比 DSH 自身：面板打开会移焦、关闭会归位，绝不把焦点丢给根节点。

## v0.4.15 — 焦点管理（v136）

### 修复

- **关闭下拉菜单后焦点丢失**：Esc、点击外部、再次点击触发按钮三条路径都会把焦点丢给页面根节点。现在关闭后回到触发按钮。
- **帮助面板打开时完全不移焦**（它没有菜单项，原逻辑只找菜单项）。
- **切换菜单后关闭会回到「上一个」触发按钮**：切换时旧菜单的清理逻辑先归位，新菜单又记下了错误的归位目标。现在按当前打开的菜单查询触发按钮，并在切换时不执行归位。

## v0.4.13 — 按钮字号统一 + 硬编码色清理（v135）

### 修复

- **浮窗底部三个按钮字号不一致**（13/11/11px）：主按钮与危险按钮的特异性更高，压过了后声明的统一规则。修正后三个按钮一致。
- 清理 6 处被后续规则完全覆盖的硬编码色（含一处红色 hover，其设计早已被弃用）。

## v0.4.12 — 阻止提示不再被浮窗遮住（v134）

### 修复

- **拦截提示会被浮窗永久遮住**：提示条与浮窗分属不同层叠上下文（有效层级 7 vs 20），把浮窗拖到下方即可完全覆盖它；实测在矮视口下浮窗的默认位置就已经重叠。现在提示条改为挂到页面根节点并独立定位，层级高于自家浮窗与浮层、低于 DSH 原生菜单，且不接收指针事件，浮在其上也不影响点击。

## v0.4.10 — 弹层不再被浮窗盖住（v133）

### 修复

- **下拉菜单在浮窗打开时点不到**：菜单与浮窗分属不同层叠上下文（菜单所在容器被限制在有效层级 7，而浮窗所在层是 20），此时把菜单的 z-index 调到多大都无效。实测浮窗与档位菜单重叠 116px，导致「关闭」一档整行不可点、「普通」只剩 7px。现在四个菜单都渲染到页面根节点，有效层级不再被压制，真实鼠标点击与中心命中均恢复到菜单项上。

## v0.4.9 — 清理永不生效的样式规则（v132）

### 修复

- 删除 7 条被完全覆盖、从未生效的样式规则（含 5 处硬编码状态色与一条与后声明完全相同的 hover），并修正一处对层叠层级的过时注释。

## v0.4.8 — 死样式与信息重复（v131）

### 修复

- 修复完成态同时显示「思考 — tok」与「（优化中断，无思考内容）」的信息重复：该 chip 现在只在「已跑完且供应商未上报」时出现。
- 删除 4 条被同选择器覆盖、从不生效的样式（含一条硬编码紫色圆点）。

## v0.4.7 — 暗色验收 + 查证轨迹可达（v130）

### 修复

- 浮窗标题行的尺寸提示恒显示 `400×320`，而拖动过程中它并不更新（拖动经动画帧直接改 DOM，不触发重绘），只在松手后停留一个静态数字，反馈价值≈零。现改为恒显示「↘ 可改大小」。
- 「拦截累计 N」在 N=0 时不再显示。
- **非极端档不再渲染查证轨迹的空态面板**（查证只属于极端档，此前任何档位只要有运行就会多出一块空面板）。
- **极端档的查证轨迹对真实用户永远在屏幕外**：新增「查证 N 步 ↓」入口，点击滚动到位。

### 新增

- 控制台调试入口暴露 `startRun`，便于复现失败路径。

## v0.4.6 — 暗色主题对齐 DSH 菜单（v128-v129）

### 修复

- 暗色下弹层表面用的是层 1 底色，比 DSH 原生菜单更暗；现在统一到 DSH 菜单的实际取值，并让浮层阴影与 DSH 菜单逐值一致（此前两个浮层还不一致）。
- 弹层入场动画移除：DSH 原生菜单没有任何动画（已从其样式源码确认，不是「已播完」的假象）。
- 浮窗圆角对齐 DSH 对话框的 18px（弹层保持 20px，与 DSH 菜单一致）。

### 说明

- 本轮建立了暗色主题的验证能力：DSH 用根元素的主题属性承载暗色，属性可跨连接保持、移除后逐值还原，不触碰用户设置。

## v0.4.5 — 浮窗按钮 hover 对齐（v127）

### 修复

- 浮窗默认按钮的 hover 由「描边变强调色 + 发光阴影」改为 DSH 的实际语言：只换背景底色，文字色不变、无阴影、无位移。

## v0.4.4 — 菜单项 hover 对齐（v126）

### 修复

- 菜单项 hover 由「强调色背景 + 强调色文字 + 左侧指示条 + 右移」改为 DSH 原生菜单的实际表现：背景换成交互底色，文字与主标签一致，无位移无阴影。

## v0.4.3 — 动效统一到 DSH 曲线（v125）

### 修复

- **插件里并存三套动效语言**（上游的回弹曲线、缓出曲线、后加的手写过渡），而 DSH 全程只用一条曲线加 0.3 / 0.18 / 0.12 / 0.1 秒的时长梯度。现全部统一，并删除 13 处 hover/active 位移与缩放（含帮助按钮 hover 旋转 8 度），DSH 的按钮完全不做位移变形。

## v0.4.2 — 去掉跨会话背景回退（v124）

### 修复

- **认不出会话时会拿「第一个会话」的历史当背景**：这会造成跨会话读取，给优化模型错误的上下文。现在认不出会话就宁可不用背景。

## v0.4.1 — 会话背景 + 暗色开关对比度 + 隐私行可达（v120-v122）

### 新增

- **会话背景**：优化模型此前只拿到单条文本，无法正确处理「上面那个函数」「继续」这类依赖上下文的输入。现在会取本会话最近的对话作为**消歧背景**（越靠后越新，最多 6 轮），并在提示中写死边界：仅用于补齐指代；原文没提到的事一律不得写进命令；与原文冲突时以原文为准。模型菜单内可随时关闭，关闭后不读取任何历史；浮窗元信息行显示已参考的轮数。系统注入的消息（运行时上下文、系统提醒、技能清单、目标轮次等）会被过滤，不当作对话历史。

### 修复

- **暗色下开关的圆点在轨道上看不见**（对比度 1.05）：圆点颜色引用了 DSH 中并不存在的变量，永远回退纯白，而暗色轨道的强调色也接近纯白。现按主题取相反的极值，四种组合的对比度分别为 5.21 / 18.9 / 8.18 / 17.45，全部达到 WCAG 对界面组件的 3:1 要求。
- **矮视口下隐私开关沉到首屏外**：低视口下弹层高度受「不遮挡触发按钮」约束被压到 266px，而内容高 414px，隐私行需滚动 148px 才可见 —— 违背「可关闭 + 明确告知」的既定约束。现隐私行粘性贴底，滚动前即可见；宽视口零副作用。

## v0.4.0 — 极端档只读查证接入真实路径（语义收窄）

### 变更

- **极端档语义收窄为「只读查证 · 仅用于消歧与校验」**：此前 `TIER_SPECS.extreme.system` 要求模型产出「分阶段执行计划」，并自行判断「这次任务是否值得让工作 AI 用 goal / todo / 计划模式跟踪」。计划工作属于 DSH 的 plan 模式，不该由提示词优化器代劳，这两项已从 system prompt 中移除；保留的是**命令结构固化**（要做什么／验收标准／硬约束／不许做什么）与**多情况预案**。
  新增的边界四条：只用于消歧与校验；不得把查到的项目事实写成用户没提过的新要求、新验收项或新技术选型；与项目实际不符时写成让工作 AI 先确认的措辞；查不到就如实留空，也不为查而查。

### 修复

- **极端档的只读工具循环此前从未在真实用户路径上运行**：`executeLiveRun()` 调用 `streamWithTools()` 时第 5 个参数（`tools`）恒为字面量 `undefined`，而带工具循环的 `runToolLoop()` 只被自检专用的 `runVersion()` 调用。因此用户在界面上选「极端」后插件并不会读取项目，`.dpo-trace` 面板也永远显示「（暂无查证动作）」。现在 `executeLiveRun()` 在 `run.tier === 'extreme'` 时改走 `runToolLoop()`，以 `resolveSessionCwd(ctx)` 得到的工作区作为项目根，并把 `relayMessage()` 的产物直接作为消息数组传入。
- **`opts.userText` 被重复包装**：`relayMessage()` 返回的本就是 `userMessageFor(...)` 的结果（消息数组），`runToolLoop()` 又对其调用一次 `userMessageFor()`，使指令正文被序列化成 `[object Object]` 送给模型（产物首句表现为「本次指令正文未收到有效内容（收到的是占位符 `[object Object]`）」）。现改为 `Array.isArray(opts.userText) ? opts.userText : userMessageFor(opts.userText)`，同时兼容自检路径的纯文本入参。

### 改进

- **查证过程实时可见**：`runToolLoop()` 新增 `onDelta` / `onReset` / `onTrace` 三个回调，经 `publishRun()` 推给前端 —— `onDelta` 透传流式增量，`onReset` 在多轮查证之间清空产出（否则各轮文本会拼接成最终结果），`onTrace` 每次工具调用后推送完整查证记录。前端 `es.onmessage` 新增 `text-reset` 与 `trace` 两个分支，`traceRows()` 改为优先读取 `store.run.trace`，探针通道的 `store.trace` 仅作兜底。
- 档位文案同步：`hint` 由「反复查证，约 20 秒以上」改为「读项目消歧，约 20 秒以上」，`help` 由「读项目真实结构 → 分阶段行动计划 + 预案（约 20 秒）」改为「读项目真实情况，仅用于确认指代（约 20 秒）」；帮助面板推荐语同步更新。
## v0.3.1 — 优化前后同屏对照 + 档位显示中文

### 修复

- **`.dpo-run-status` 的档位显示英文 id**：运行状态行（「档位 advanced · 已完成 · 首字 591ms」）用的是 `run.tier` 原始 id，而非 `TIERS` 的 label —— 同一屏的 `.dpo-overlay-src` 却已显示中文「发送：审查」，两者并列时明显割裂。同源问题还存在于优化完成通知：`showNotice("已按 " + run.tier + " 档优化结果发送")`。两处均改为 `((TIERS.find((x) => x.id === run.tier) || {}).label || run.tier)`，与 `.dpo-overlay-src` 的处理方式一致。

### 改进

- **「你的原文」提到结果之后，优化前后可同屏对照**：此前 `.dpo-orig{order:5}` 排在滚动区末位，而优化完成后用户的第一个动作往往就是「对照原文，确认有没有改坏、有没有丢关键信息」。真实 done 态量测：原文 `top 444`、textarea `bottom 204` → 相距 **240px**，`origVisibleNow = false`；必须滚动才能看到原文，而滚到原文时 textarea 已离开屏幕，无法对照。
  调整顺序为 `元信息(1) → 结果(2) → 原文(3) → 思考(4) → 查证(5)` 后：原文 `top 220`、相距 **16px**、`origVisibleNow = true`，且原文完整落在首屏内（`orig.bottom 298 < clientH 393`）。

### 实现要点

- 只调整 CSS `order`，不动 DOM 结构（`.dpo-orig` 仍是 `.dpo-overlay-scroll` 的直接子级），因此不影响 `runPanes()` / `reviewPane()` 的任何渲染逻辑，也不触碰 `.dpo-review` 内部那三处 order 依赖。
- 保留 `.dpo-run`（思考）与 `.dpo-trace`（查证）的相对次序：它们属于过程信息，排在结果与原文之后。

## v0.3.0 — 优化结果一键复制

### 新增

- **一键复制优化结果**：浮窗「检查后提交」标题行右侧新增「复制」按钮。优化结果通常数百字、且可能已被手工编辑，此前只能手动全选 —— 插件此前完全没有 clipboard 支持。

### 实现要点

- `copyResult(btn)`：优先 `navigator.clipboard.writeText`；被拒或不可用时回退 `document.execCommand("copy")`，回退前保存并恢复 textarea 的原选区。
- 反馈：成功后按钮变「已复制」+ `data-copied="1"`（品牌色 12% 底 + 品牌色字），1.4s 后自动还原；失败显示「复制失败」。
- 按钮置于 `.dpo-pane-title` 内部、`margin-left:auto` 顶到行尾。**没有包一层 wrapper**：`.dpo-review` 的直接子级被 order 规则依赖（`.dpo-review > .dpo-pane-title{order:2}`），加 wrapper 会一并破坏 `.dpo-regen-ask` / `.dpo-review-text` 的排序。
- 样式遵循 DSH 控件语言（默认无边框无底色、hover 才浮出浅灰底）：`border:0`，起始色 `label-tertiary`，hover 用 `interactive-bg-hover`，成功态用 `state-business-primary` —— 全部为 DSH 语义 token，无硬编码色值。

### 验证

- 参数正确性（mock clipboard）：`capturedEqualsTextarea = true`
- 真实 clipboard 路径：不抛异常、按钮进入「已复制」态
- 1400ms 后自动还原
- clipboard 被拒时确实走回退分支（`execCommand` 被执行）
- computed style：`borderTop 0px`、`color rgb(129,133,140)`、`padding 4px 10px`、`margin-left:auto` 解析为 179.125px、按钮右缘与标题行右缘间距 0px

## v0.2.0 — fork（基于上游 v0.1.1-beta.1）

> 🔀 本仓库是 [`LessXi/dsh-prompt-optimizer`](https://github.com/LessXi/dsh-prompt-optimizer)，上游为 [`WestFox-AwA/dsh-prompt-optimizer`](https://github.com/WestFox-AwA/dsh-prompt-optimizer) 的 `v0.1.1-beta.1`。包名改为 `@lessxi/dsh-prompt-optimizer`，可与上游并行安装。**上游的功能与设计意图全部保留**，以下均为增量。

### 修复的上游缺陷

- **档位 / 发送下拉按钮点了打不开**：`onClick` 里 `store.tierPopOpen = !store.tierPopOpen` 之后，紧跟的四路互斥块又写了 `store.tierPopOpen = false`（把自己也置回），弹层永远无法打开。`Shift` 之类的操作无法规避，实际是「按钮完全无响应」。
- **窄屏「档位」按钮弹出的是模型列表**：`data-dpo="tier-collapsed"` 的 `onClick` 写的是 `store.modelPopOpen`，语义错位。
- **`--dsw-alias-bg-l1` 在 DSH 中不存在**：插件引用了 8 个 `--dsw-*` token，只有这一个在 `@deepseek-ai/dsh-client-ui-theme` 里查无此名，而被用在 4 处；回退值是暗色 `#141414`，浅色主题下渲染成**黑色编辑框**。改用真实存在的 `--dsw-alias-bg-layer-1`。
- **弹层互斥不完整、且没有「点击外部 / Esc 关闭」**：四个弹层各写各的互斥（模型按钮零互斥），点两个不同按钮会**同时悬空两个弹层**；打开后只能点各自的「关闭」。新增 `closePops(one)` 单一互斥入口 + 全局 `pointerdown`/`keydown` capture 监听（判定时排除弹层自身与四个触发按钮，否则会出现「点不掉」）。
- **`.dpo-overlay-scroll` 的子项会被压成 1px**：flex column 容器的直接子项默认 `flex-shrink:1`，而 `.dpo-trace` 的 `overflow:auto` 让 `min-height:auto` 解析为 0 —— 内容溢出时整块查证动作只剩一条边框（实测 8 行、`offsetHeight:1`、0 行可见）。补 `flex:0 0 auto`。
- **矮视口下查证动作只有 1 行可见**：`@media (max-height:640px)` 里的 `max-height:72px` 是照早期 padding（表头 24px + 行 19px）估的，padding 被放大到 29px/23px 后没同步，8 行里只有 1 行完整可见。改为 `min(150px,26vh)` 并给表头 `position:sticky`。
- **error 态同一屏两组重复出口**：错误行自带「重试 / 恢复默认模型并重试」，底部常驻操作栏也有「重试 / 默认模型重试 / 按原文发出」，`onClick` 逐字节相同；且 `.dpo-run-error` 的 `word-break:break-all` 会**继承给子按钮**，中文被压成竖排（一个字一行）。
- **模型弹层的标题行缺失**：删除窄屏专用 `.dpo-pop-sliders` 时把同一可见块里的 `.dpo-pop-head` 一并删掉了。
- **done 态出现没有标题的孤立原文片段**：`setOverlay` 是 `Object.assign` 合并语义、不清除未提及字段，run 态写入的「原文前 80 字」在 review 态残留。

### 视觉与交互（与 DSH 本身对齐）

- 控件行胶囊**逐属性**对齐 DSH composer：高度 30→**28px**、圆角 8→**24px**、字号 12→**13px**、字重 400→**500**、padding `0 4px 0 8px`、去边框。
- 下拉箭头 **1:1 复刻** DSH 官方 chevron（CSS `mask-image`，624 字符 path，颜色继续跟随主题而非写死）。
- 档位 / 发送由横向滑块改为**下拉**：4 档与 2 档都是离散取值，滑块是「连续量」隐喻；DSH 原生对同类选择一律用下拉。
- 弹层配色对齐 DSH 原生菜单：**纯白底**（不用毛玻璃）、圆角 **20px**、条目圆角 10px。
- 帮助弹层**去蓝**：行标题原为品牌蓝 + 600 字重（一屏 13 行里 9 行是蓝的），改为 `label-primary` + 500，符合 DSH「行标题 primary / 说明 secondary / 分组标题 caption」的灰阶层级 —— 彩色只给主操作。
- 浮窗：标题栏三列 grid（修复回退按钮被甩到最右列）、查证动作表头 sticky + 底部渐隐、长内容 `.dpo-pane` 用 `mask-image` 做 26px 渐隐（`.dpo-pane` 是 `overflow:auto`，绝对定位伪元素会随内容滚动，mask 不会）、原文摘要 `min(50vh,360px)`。

### 功能

- **「你的原文」**：done 态补回原文摘要并加标题 —— 「优化前后可对比」正是提示词优化器的核心价值。
- **文案收敛到单一数据源**：`TIERS` / `PERMISSIONS` 各补 `hint`（弹层短说明）与 `help`（帮助长说明）字段，弹层与帮助面板共用。原先三处各写一套，且帮助面板用的是「需要审查 / 自动输出」等与界面标签对不上的旧文案。
- **键盘可达**：弹层内 `ArrowDown`/`ArrowUp`/`Home`/`End` 环绕导航，打开时把初始焦点交给当前选中项。
- **无障碍**：下拉按钮补 `aria-haspopup`/`aria-expanded`，浮窗补 `role="dialog"` 与 `aria-label`，运行状态行补 `aria-live="polite"`。

### 宿主端（lib/index.js）

- `loadPluginState` 对 `tier`/`permission` 补**白名单校验**：原先只判 `typeof === 'string'`，而同一文件的 `sanitizePerSession` 却严格用 `TIER_IDS.has()`；非法值经 `GET /state` 进入客户端后控件行渲染成空字符串，且会被 POST 写回持久化。
- `savePluginState` 改**原子写**（写 `.tmp` → `renameSync`），失败时把 `saveError` 带进状态对象，不再静默。
- `readJson` 的损坏留痕经 `GET /state` 的 `readError` 字段暴露；读盘成功时清除留痕（否则修好的文件会一直报警，比不报警更糟）。原先 `catch { return fallback }` 静默吞掉，配合客户端 `.catch(() => {})` 形成**端到端静默失败**。
- `mirrorToSettings` 的 `settingsStatus` 不再随保存次数无限追加 `" (mirror-skipped)"`。
- `liveRuns` 补保留上限与 `pruneLiveRuns` 回收，终止态打 `endedAt` 时间戳（原先长会话内存单调增长）。

### 与上游的差异范围

```
lib/client.js    | 763 ++++++++++++++++++++++++++++++++++------  （主题层 / 交互层 / 浮窗层 / 文案与可达性）
lib/index.js     |  58 +++-                                    （状态层健壮性）
package.json     |   4 +-                                     （name / version）
cordis.patch.yml |   2 +-                                     （bundle entry name）
```

上游保留的文件：`ACCEPTANCE.md`、`DELIVERY.md`、`evidence/`（均为 v0.1.1-beta.1 的验收与自检留痕）。**正文一字未改**；仅在两份文档顶部各加了一段「fork 差异提示」，逐条指出本文描述的上游形态（档位/权限滑块、胶囊模型、玻璃模糊与弹簧入场、三档而非四档等）与当前 fork 的差异，以免照原文验收失败。

## v0.1.1-beta.1 — 2026/09/11

作者：啃轮胎的西狐

首个对外分享版本。核心能力：

- **发送接管**：捕获阶段拦截回车与发送按钮（`Shift+Enter`、`/` 命令、空草稿、卡片外回车一律放行）。
- **传话者语义**：优化 AI 明确是"把用户意思转达给工作 AI"的传话器 —— 不回答用户、不替用户干活、不向用户提问；产出是**可直接发送的命令正文**，无「优化后的提示词 / 改动说明」这类元话语。
- **三档强度**：普通（语言精确化，约 3 秒）／高级（补"显然需要"的约束与验收，约 20 秒）／极端（只读查证项目结构 → 分阶段行动计划 + 验收标准 + 多情况预案，约 20 秒）。
- **两种权限**：需要审查（产出可编辑，确认后发送）／自动输出（完成即发送；失败也按原文发出，绝不静默吞消息）。
- **迷你窗**：可拖动、可改尺寸（记忆）、按会话隔离、常驻底部操作栏（窗口再小按钮也不消失）、思考/产出双通道流式 + 流式光标 + 自动跟随滚动、**思考 token 计数**。
- **按会话独立的档位与权限**：A 会话的设置不影响 B。
- **模型独立**：优化模型与对话模型互不影响；目录预热 + 双层缓存 + 单家超时，死模型自动回退。
- **使用帮助**：控件行 `?` 面板（怎么用 / 档位 / 权限 / 迷你窗按钮 + 推荐组合）。
- 测试与证据：`ACCEPTANCE.md` 逐格验收清单；`evidence/` 内为自检报告、三档对照、几何与产物形态回归等机器留痕。

### 已修复的典型缺陷（详见 `ACCEPTANCE.md`）

- 回退 / × 点击无效（pointerdown 的 `preventDefault` 抑制了 click）。
- 审查态看不到确认提交 / 重新生成（浮层内容被裁且不可滚动）。
- 浏览器缩小后弹窗出现在不可见坐标（开窗未夹紧）。
- 开启档位后"无法发送"（优化模型不可用 + 失败被静默吞掉）。
- 弹窗不再显示（React #310：hook 写在 early return 之后）。
- 模型目录加载不出来（客户端从未拉取 `/models`）。
- 右下角拖不动改尺寸（手柄被常驻底栏盖住 + `onSizeUp` 丢掉最后一次位移）。
- 优化 AI 误以为在跟用户对话（角色与用户消息都缺少"传话"框架）。
