# 交付说明 · @dsh-external/dsh-prompt-optimizer

> 一句话：**在 DSH Web 输入框接管"发送"这一步**——按下回车/发送时不直接发出，而是按你选的档位把提示词优化成精准可执行的指令，全程在可拖动的迷你视窗里可见、可审查、可重新生成、可一键回退。

> ⚠️ **fork 差异提示（LessXi fork · 当前 0.4.33）**：本文件是上游 `v0.1.1-beta.1` 的原始交付说明，**保留原样以存历史**。以下两点与当前 fork 不一致，其余能力描述仍然成立：
>
> | 原文位置 | 本文描述（上游） | 本 fork 现状 |
> |---|---|---|
> | 标题 | 包名 `@dsh-external/dsh-prompt-optimizer` | 本 fork 为 **`@lessxi/dsh-prompt-optimizer`**（当前 **0.4.33**），可与上游并行安装 |
> | 「一、它做了什么」→ 三档优化 | 列了**三档** | 实为**四档**：关闭 / 普通 / 高级 / 极端（「关闭」＝完全不拦截，恢复原生发送）。右侧的优化权限为**两档**：审查 / 自动 |
> | 「一、它做了什么」→ 视觉与动效 | v51 精致化视觉层：玻璃模糊、弹层上浮缩放入场、滑块换挡弹跳、hover 缩进与选中打勾 | 已全部调整：毛玻璃去掉（改实色，逐值对齐 DSH 菜单）；弹层入场动画移除（DSH 原生菜单无动画）；**全部位移/缩放/旋转删除**，动效统一到 DSH 的单一曲线与 0.3 / 0.18 / 0.12 / 0.1 秒时长梯度；滑块已改为下拉菜单 |
> | 未涉及 | 下拉菜单直接渲染在控件行内 | 四个菜单已 **portal 到页面根节点**（否则迷你窗会盖住菜单） |
> | 未涉及 | 界面文案全中文 | 现**跟随 DSH 的界面语言**（`zh` / `en`） |
>
> 完整的逐版本变更见 [CHANGELOG.md](CHANGELOG.md)，逐格验收请注意 [ACCEPTANCE.md](ACCEPTANCE.md) 顶部的同款提示。

## 一、它做了什么

| 能力 | 说明 |
|---|---|
| **发送接管** | 捕获阶段拦截回车与发送按钮；档位≠关闭时消息不发出，改为打开迷你视窗。`Shift+Enter` 换行、`/` 命令、空草稿、仅附件、卡片外回车一律放行（与官方一致） |
| **三档优化** | **产出＝可直接发给工作 AI 的命令正文**（用你的口吻下指令，零元话语：没有"优化后的提示词/改动说明/边界"这类写给你看的话）。普通=语言精确化（禁新增需求）；高级=轻上下文（真实目录树/关键文件）+ 把"显然要的"写成对工作 AI 的要求；极端=只读工具循环（read/glob/grep，限项目内、不写盘），**仅用于把命令里的模糊指代落到实处**（读到的项目事实不得写成用户没提过的新要求）+ 验收标准/多情况预案。回归：`evidence/output-shape-check.json` 3/3（元话语 0 命中、祈使指令、首行即命令） |
| **迷你视窗** | `shell.overlay` 内可拖动浮层（跟手、越界夹紧、不挡页面）；`思考`/`产出` 双通道流式（SSE）；审查态可编辑文本（超长滚动）+ 蓝色「确认提交」（与发送键同色）+ 红色「重新生成」；左上「回退」二次确认 → 中止后端请求 + 关浮层 + **不发送** + 原文一字不动 |
| **模型选择** | 与右下角官方选择器**同源**（10 组 / 35 模型实测），只影响优化、不动对话模型；不可用模型给人话提示 + 一键回默认 |
| **配置持久化** | 档位/权限/模型/浮层几何落盘 `~/.dsh/prompt-optimizer.json`，跨重启保持（已实测：重启后 `state-loaded` 读回 `extreme/auto/null`） |
| **视觉与动效** | v51 精致化视觉层：玻璃模糊 + 双层阴影 + 内侧高光 + 统一圆角/细滚动条；弹层上浮缩放入场、浮层呼吸状态点、运行状态流光、思考/产出流式光标与自动跟随滚动、滑块换挡弹跳、hover 缩进与选中打勾、「会话当前」芯片；`prefers-reduced-motion` 全关。由 `css-probe` 自证 12 项全 true |

## 二、装在哪、怎么重启都在

- 插件目录：`~/.dsh/plugins/dsh-prompt-optimizer`
- 已装入 profile：`~/.dsh/profiles/web/package.json`（`dependencies` 用 `link:` 指向插件目录 + `bundles` 含本包）⇒ **重启 DSH 后由 bundles 自动装配**（已实测：重启后路由可用、客户端自动装载、配置保持）
- 卸载：`dev_uninject_plugin {match:'dsh-prompt-optimizer'}`（实测卸载后：路由 404、页面轮询停止、beacon +0；重注入自动恢复）

## 三、验收怎么走

- 人看版：**`ACCEPTANCE.md`**（本目录）——18 格「动作 → 应看到 → 应感受」矩阵 + 前置条件 + 边界 + 模型选择 + 引擎能力亲手触发方式 + 已知边界
- 机器版：`evidence/` 下
  - `selftest-report.json`：探针 19 步（E1–E6 拦截/IME/按钮/对照/交付正控；F1–F3 三控件落位/置灰联动/零位移；G 放行白名单+fail-open；I1 查证 trace 可见；J1 拖动/边界/穿透/清理；K1 流式双通道/失败态/重试；L1 审查态/配色/提交链路；M1 回退二次确认；N1 模型弹层同源/独立/生效；P1 落盘/兜底/一致性；H1 真·A/B 基线）
  - `tier-compare.json` 三档对照（399/1729/3960 字）· `context-check.json` 轻上下文 A/B（13 vs 0 真实引用）· `tool-loop.json` 工具循环（9/9 引用真实、行级回读 4/4 一致、超限收敛）· `rerun-check.json` 方向化重跑（更简洁 Δ-790/630、独立会话不串台、回退 `subs=0`）
  - `client-beacon.jsonl` 页面遥测（真实按键/点击判定）· `host-events.jsonl` 宿主权威落库流水

## 四、取舍与已知边界（诚实清单）

1. **窄窗口**：做了响应式收缩（断点 1480/1240/1080），但**未**做"折叠成胶囊+弹层"；极窄窗口下工具行可能换行。取舍原因：优先保证控件不被压扁、官方控件不位移（实测 `drift=0`）。
2. **三项存在插件自有文件**，未并入平台 settings：DSH 的 `settings.register` 要求**可调用的 schemastery schema**，而 `schemastery` 在 profile 侧不可解析（`zod` 可解析但不可调用）⇒ 并入需给插件增加包依赖（当前刻意零运行时依赖）。
3. **探针（`evidence/` 里的自检装置）**：开发期工具，会在某些步骤里真的发起一次优化/提交一条标记消息并立刻撤销。历史上留下过 6 条标记消息（均定位为探针时序缺陷：标记前缀未统一、idle 提交立即落库、L1 与看门狗竞态、卸载期路由不可用）。**现已四道防线**：标记统一 `DPO-` 前缀 + 宿主看门狗按前缀清扫 + `running` 门禁（idle 跳过破坏性提交）+ 破坏性测试前停探针。**建议最后把探针停掉**：`evidence/cmd.json` 置 `{ "run": null }`（当前已是）。
4. **探针使用**：想自检时把 `cmd.json` 写成 `{ "run": "selftest", "token": "<新值>" }`，页面会在 2 秒内跑完 19 步并把报告写回 `evidence/selftest-report.json`（页面需处于本会话且输入卡片可见，否则按设计整轮中止）。
5. **时长受模型/推理档影响**：基准样本为 `deepseek-official / deepseek-flash + effort=max`（普通 2.8–3.4s / 高级 21–25s / 极端 18–20s）。

## 五、后续可选打磨（按价值排序）

1. 窄窗口折叠成 `优化·普通 ▾` 胶囊 + 弹层（弹层里放两条大滑块）——彻底消除换行
2. 弹层高亮"当前会话模型"（需接入 `modelSelection` 投影；现只高亮显式选择，避免误高亮）
3. 错误行两个按钮的措辞与权重再理一轮（重试 / 恢复默认模型并重试）
4. 探针瘦身为"3 步冒烟"（骨架在位 / 回车拦截 / 三控件），把全量 19 步留作发布前回归
