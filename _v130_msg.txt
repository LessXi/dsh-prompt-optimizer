v130: dark-verification pass -> 4 UI defects fixed (trace jump entry + zero-info removal)

- v130a head-hint no longer shows the panel pixel size (400x320). Resize does not
  emit during drag, so the number only ever appeared after the drag ended and then
  stayed forever - zero feedback value. The default hint "-> resizable" is the
  useful affordance.
- v130b meta row "intercepted N times" now only renders when N >= 1 (0 was noise).
- v130c trace empty-state panel is no longer rendered outside the extreme tier
  ("verification" only exists in extreme). Scroll content 646 -> 495 = exactly
  the 151px panel height.
- v130d window.__DPO__ now exposes startRun, same family as probe/arm/disarm,
  which makes the error state reproducible (real host error NO_ADAPTER verified).
- v130e extreme tier gets a "verify N steps" jump entry: measured popover 400x320,
  scroll viewport 216px, content 646px -> the trace panel needed 372px of extra
  scrolling, i.e. the user could never see what the extreme tier verified.
  After click: scrollTop 6 -> 431, traceInside false -> true.
- version 0.4.7 (PLUGIN_VERSION + package.json)

Verified: dark error state 28 elements zero light-residue, contrast textVsOwnBg 6.37
/ textVsOverlay 4.77; extreme trace 7 steps readable in dark (traceColor =
label-primary #f9fafb); light-theme basic regression green on 1264x630 / 984x510 /
1284x810 with no side effects.
